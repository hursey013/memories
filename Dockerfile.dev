FROM node:22-alpine
WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy your source
COPY src ./src

# Development environment
ENV NODE_ENV=development

# Run using Node’s built-in watcher
CMD ["npm", "run", "dev"]
